import pluginConfig from '../../plugin.config.json';

export const REQUEST_DOMAIN = '' || pluginConfig.siteDomain;

// Fill in the request domain name of the server side.
export const BASE_URL = '';
export const AUTHORIZATION = 'authorization';
export const THEME_ATTRIBUTE = 'theme-mode';
